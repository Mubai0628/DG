{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop shell permissions for local web-table-to-CSV flow.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

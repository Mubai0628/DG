# browser-extension

Reserved for the future Chromium Manifest V3 extension.

DW-P0A-001 intentionally does not add a manifest, browser permissions, content scripts, activeTab capture, navigation, click, submit, fetch, cookie, localStorage, sessionStorage, or password-field access. The v0.1.0 browser path will be implemented later as user-triggered visible DOM/table read only.

# conformance

Reserved for DeepSeek compatibility and behavior tests.

Default CI must run without a DeepSeek API key. Real API probes will be opt-in in later tasks and must never run as part of the default dry path.

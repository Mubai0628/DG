# v0.19 MCP Tool Invocation Proposal Post-Release Review

Release:

- tag: `v0.19.0-mcp-tool-invocation-proposal-rc.1`
- title: `MCP tool invocation proposal and approval design, no tool execution`
- prerelease: yes

## Checks

- Scoped P0W checks covered App typecheck/tests, focused MCP tool proposal
  runtime tests, boundary checks, secret checks, and diff checks.
- Stage-end full gates covered lint, typecheck, runtime tests, App tests,
  conformance dry/live skip, App smoke/preflight/QA, Tauri build/check,
  browser-extension build/test, web-table eval, v0.1 slice verification,
  release smoke, boundary checks, secret checks, and `pnpm verify:ci`.
- GitHub Actions passed on `main` and on the release tag.
- Manual GUI QA is documented in
  `docs/mcp-tool-invocation-proposal-manual-qa.md`; do not claim it as run
  unless a human performs the checklist.

## Current Capabilities

- Convert: `web_table_to_csv` remains the real conversion flow.
- App-side approved apply / rollback exists and remains human-approved,
  typed-confirmed, checkpointed, summary-only, and rollbackable.
- Git/shell verification safe lanes exist and remain fixed plus summary-only.
- Project Knowledge supports human-reviewed workspace-local summaries.
- Capability Host descriptors support descriptor-first metadata previews.
- MCP read-only discovery supports fixed-profile metadata discovery without
  reading resource content by default.
- MCP tool invocation proposals support schema, input risk classification,
  simulated result summaries, broker planning, App read-only proposal display,
  redaction audit, and smoke coverage.

## Disabled Capabilities

- real MCP `tools/call`
- mutating MCP tools
- plugin code execution
- skill runtime execution
- native bridge
- desktop action
- arbitrary shell/process spawn
- broad PermissionLease
- autonomous agent tool execution
- raw MCP tool arguments or output in App, events, replay, or release docs

## Known Warnings

- GitHub Actions reports the repository-level Node.js 20 deprecation warning
  for third-party actions forced onto Node.js 24.
- Tauri may warn that the bundle identifier ends with `.app`.
- Vite may warn about large chunks during App build.

## Release Hygiene

- No raw source, raw diff, raw prompt, raw response, raw MCP output, raw MCP
  arguments, API key, Authorization, bearer token, or secret value is included
  in v0.19 summary artifacts.
- Generated build, eval, conformance, and temporary artifacts remain ignored.

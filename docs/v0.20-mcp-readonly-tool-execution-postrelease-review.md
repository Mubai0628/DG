# v0.20 MCP Read-only Tool Execution Post-Release Review

Release:

- tag: `v0.20.0-mcp-readonly-tool-execution-rc.1`
- title: Controlled MCP read-only tool execution MVP
- commit: `fef82b1`
- pre-release: true

Checks:

- Scoped checks passed before the RC polish commit.
- Stage-end full gates passed before release.
- Manual GUI QA completed for Convert, Event / Replay, MCP read-only connection, and fixed read-only tool call surfaces.
- GitHub Actions status: green for main and tag at release time; re-check on any later branch update.

Current capabilities:

- Convert: `web_table_to_csv` remains the real local conversion flow.
- Event Log / Replay remains summary-only.
- Record Draft Event remains the App/Tauri local summary-event write path.
- App-side approved apply / rollback exists behind human approval and exact confirmations.
- Git / shell verification safe lanes exist as fixed, summary-only lanes.
- Project Knowledge / Production Memory remains human-reviewed and summary-only.
- Capability Host descriptor preview exists.
- MCP read-only connection and metadata discovery exist.
- MCP read-only tool execution exists only for fixed allowlist, explicit approval, bounded output, summary-only events, and replay summaries.

Disabled capabilities:

- Mutating MCP tools.
- Arbitrary MCP tool calls.
- Plugin code execution.
- Skill runtime execution.
- Arbitrary shell/process spawn.
- Native bridge.
- Desktop action.
- Broad PermissionLease.
- Autonomous agent tool execution.

Known warnings:

- Tauri bundle id warning.
- Vite chunk-size warning.
- GitHub Actions Node.js 20 deprecation annotation if present.

Release hygiene:

- No raw MCP tool output persistence.
- No raw tool args persistence.
- No raw plugin or skill package content.
- No API key, Authorization, bearer, or secret persistence.
- Generated artifacts and conformance outputs were not committed.

Next stage:

- P0Y moves from MCP read-only tool execution to Plugin / Skill Sandbox MVP with descriptor-first, manifest-first, sandbox-contract-first governance.

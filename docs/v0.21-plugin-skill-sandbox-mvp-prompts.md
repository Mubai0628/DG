# v0.21 Plugin / Skill Sandbox MVP 阶段提示词

任务目标：完成所有小阶段任务。

本阶段目标版本：

```text
v0.21.0-plugin-skill-sandbox-mvp-rc.1
```

阶段主题：

```text
Plugin / Skill Sandbox MVP
```

核心目标：在 v0.20 MCP read-only tool execution 的基础上，把 plugin / skill 纳入 descriptor-first、manifest-first、sandbox-contract-first 的能力治理体系。v0.21 只允许 metadata scanning、descriptor integration、simulation / dry-run 或 built-in safe skill 的受限模型；不允许任意插件代码执行、不允许任意 skill runtime、不允许 native bridge、desktop action、arbitrary shell/process spawn，也不打开 broad PermissionLease。

---

## 0. 当前状态

当前已发布：

```text
v0.20.0-mcp-readonly-tool-execution-rc.1
```

Release title:

```text
v0.20.0-mcp-readonly-tool-execution-rc.1 — Controlled MCP read-only tool execution MVP
```

当前能力边界：

- `web_table_to_csv` Convert 是真实 conversion flow。
- App-side approved execution 已支持 human-approved apply / rollback。
- Git / shell verification safe lanes 已存在，fixed + summary-only。
- Project Knowledge / Production Memory 已存在，human-reviewed + summary-only。
- Capability Host descriptor preview 已存在。
- MCP read-only connection 已存在。
- MCP read-only tool execution 已存在，但仅 fixed allowlist、explicit approval、bounded output、summary-only events。
- 当前没有 plugin code execution。
- 当前没有 arbitrary skill runtime。
- 当前没有 mutating plugin / skill tools。
- 当前没有 native bridge。
- 当前没有 desktop action。
- 当前没有 arbitrary process spawn / shell。
- 当前没有 autonomous agent tool execution。

---

## 1. v0.21 阶段硬边界

整个 v0.21 阶段必须保持：

```text
no arbitrary plugin code execution
no arbitrary skill runtime execution
no plugin installation with execution
no skill package execution
no mutating plugin tools
no mutating skill tools
no native bridge
no desktop action
no arbitrary process spawn
no arbitrary shell
no broad PermissionLease
no autonomous agent tool execution
no raw plugin package content in events
no raw skill package content in events
no raw args / raw outputs in events
no API key / Authorization / bearer / secret persistence
```

允许：

```text
plugin manifest parsing
skill manifest parsing
package metadata scanning
capability descriptors
risk classification
redaction audit
simulation / dry-run result models
built-in safe skill simulation only if non-mutating and no code execution
App read-only surfaces
summary-only events/previews
```

---

## 2. Git / 测试工作流

每个普通小阶段开始时运行：

```powershell
git status --short
git status -sb
git log --oneline origin/main..HEAD
```

如果存在与当前任务无关的 dirty changes，停止并报告。

每个普通小阶段结束时：

```powershell
git diff --check
git diff --cached --check
```

然后只 stage 本任务文件并 commit。

普通小阶段禁止：

```text
git push
git tag
gh release create
pnpm test                         # 无过滤全量
pnpm verify:ci
pnpm release:smoke
pnpm app:build
browser-extension build/test
eval:web-table-to-csv
full conformance matrix
```

阶段收口 / RC 小阶段才运行 full gates、push、tag、release。

Runtime tests 必须优先使用显式 Vitest 文件路径，例如：

```powershell
pnpm exec vitest run runtime/test/<specific>.test.ts
```

---

## 3. 小阶段列表

必须按顺序完成：

1. `DW-P0X-009` — v0.20 post-release review + P0Y roadmap lock
2. `DW-P0Y-001` — Plugin / Skill Sandbox ADR + Threat Model + Implementation Gate
3. `DW-P0Y-002` — Plugin Manifest Schema, no runtime execution
4. `DW-P0Y-003` — Skill Manifest Schema, no runtime execution
5. `DW-P0Y-004` — Package Metadata Scanner, no code execution
6. `DW-P0Y-005` — Sandbox Contract + Built-in Safe Skill Simulation, no arbitrary execution
7. `DW-P0Y-006` — Capability Broker Integration with Plugin / Skill Metadata
8. `DW-P0Y-007` — App Plugin / Skill Host Surface, read-only
9. `DW-P0Y-008` — Plugin / Skill Redaction / Boundary Audit + Smoke
10. `DW-P0Y-009` — v0.21 RC polish + full gates + push/tag/release

---

# DW-P0X-009 — v0.20 post-release review + P0Y roadmap lock

## Goal

Create v0.20 post-release review and lock the P0Y Plugin / Skill Sandbox MVP roadmap.

## Scope

Docs / roadmap / docs-lock only.

No runtime feature. No App UI feature. No plugin execution. No skill runtime.

## Files

Add:

- `docs/v0.20-mcp-readonly-tool-execution-postrelease-review.md`
- `docs/p0y-plugin-skill-sandbox-roadmap.md`
- `docs/p0y-001-plugin-skill-sandbox-gate-plan.md`

Update:

- `docs/README.md`
- `README.md`
- `app/test/desktop-shell.test.ts` with docs-lock test

## Post-release review must include

Release:

```text
tag: v0.20.0-mcp-readonly-tool-execution-rc.1
title: Controlled MCP read-only tool execution MVP
commit: if unknown locally, write to be verified
pre-release: true
```

Current capabilities:

- web_table_to_csv Convert
- App-side approved apply / rollback
- Git / shell verification safe lanes
- Project Knowledge / Production Memory
- Capability Host descriptor preview
- MCP read-only connection
- MCP read-only metadata discovery
- MCP read-only tool execution with explicit approval
- MCP read-only tool summary events / replay

Disabled capabilities:

- mutating MCP tools
- arbitrary MCP tool calls
- plugin code execution
- skill runtime execution
- arbitrary shell/process spawn
- native bridge
- desktop action
- broad PermissionLease
- autonomous agent tool execution

Known warnings:

- Tauri bundle id warning
- Vite chunk-size warning
- GitHub Actions Node.js 20 deprecation annotation if present

## P0Y roadmap must define

P0Y goal:

```text
Move from MCP read-only tool execution to Plugin / Skill Sandbox MVP.
Descriptor-first, manifest-first, sandbox-contract-first.
No arbitrary plugin code execution.
No arbitrary skill runtime.
No native bridge.
No desktop action.
```

P0Y tasks:

1. Plugin / Skill Sandbox ADR + Threat Model + Implementation Gate
2. Plugin Manifest Schema
3. Skill Manifest Schema
4. Package Metadata Scanner
5. Sandbox Contract + Built-in Safe Skill Simulation
6. Capability Broker Integration
7. App Plugin / Skill Host Surface
8. Redaction / Boundary Audit + Smoke
9. RC polish + release

Explicitly defer:

- arbitrary plugin execution
- arbitrary skill runtime execution
- mutating plugin / skill tools
- desktop action
- native bridge
- broad PermissionLease
- autonomous agent tool execution
- arbitrary process spawn / shell

## Commands

```powershell
pnpm lint
pnpm app:test
git diff --check
git diff --cached --check
```

## Commit

```text
docs: lock p0y plugin skill sandbox roadmap
```

## Completion report

```text
任务：DW-P0X-009
状态：

文件变更：
- ...

运行命令：
- scoped docs/app checks:
- skipped full gates:
- git diff --check:

Post-release Review 摘要：
- release:
- checks:
- current capabilities:
- disabled capabilities:
- known warnings:

P0Y Roadmap 摘要：
- goal:
- tasks:
- deferred:
- next task:

关键不变量验证：
- docs-only:
- no new feature capability:
- no plugin/skill execution:
- no native bridge:
- no desktop action:
- no arbitrary shell/process:
- v0.1 web_table_to_csv unaffected:

Git 本地提交：
- commit hash:
- commit subject:
- git status --short:
- git log --oneline origin/main..HEAD:

未完成/阻塞：
- ...

下一建议任务：
- DW-P0Y-001 Plugin / Skill Sandbox ADR + Threat Model + Implementation Gate
```

---

# DW-P0Y-001 — Plugin / Skill Sandbox ADR + Threat Model + Implementation Gate

## Goal

Define plugin / skill sandbox strategy before any manifest or runtime implementation.

## Scope

Docs / design only.

## Files

Add:

- `docs/adr/0011-plugin-skill-sandbox.md`
- `docs/plugin-skill-sandbox-threat-model-v0.20.md`
- `docs/plugin-skill-sandbox-implementation-gate-v0.20.md`
- `docs/p0y-002-plugin-manifest-schema-plan.md`

Update:

- `docs/README.md`
- `app/test/desktop-shell.test.ts`

## ADR must decide

- Plugins and skills enter the system as manifests first.
- Manifest metadata may be parsed and risk-classified.
- No plugin code execution in P0Y.
- No arbitrary skill runtime in P0Y.
- Built-in safe skill simulation may be allowed only if no arbitrary code execution and summary-only output.
- All plugin / skill capabilities must pass Capability Broker risk classification.
- External package metadata must be redacted and summary-only.
- No raw package file contents in events.
- No plugin installation that executes lifecycle scripts.
- No native bridge.
- No desktop action.
- No arbitrary process spawn or shell.

## Threat model must cover

- malicious manifest
- dependency confusion
- package lifecycle scripts
- hidden executable payload
- prompt injection in metadata
- secret leakage in metadata
- oversized package metadata
- unsafe capability claims
- tool/capability spoofing
- sandbox escape
- path traversal
- registry URL / download risk
- event/replay poisoning
- approval bypass
- broad PermissionLease bypass

## Implementation gate must be testable

Gate categories:

- manifest schema safety
- package metadata safety
- capability descriptor safety
- sandbox contract safety
- redaction safety
- broker/risk safety
- App UI safety
- CI/boundary safety

Must explicitly say:

```text
Do not implement arbitrary plugin code execution until P0Y gates are satisfied and a later execution-specific ADR is accepted.
Do not implement skill runtime execution in P0Y.
```

## Commands

```powershell
pnpm lint
pnpm app:test
git diff --check
git diff --cached --check
```

## Commit

```text
docs: add plugin skill sandbox adr
```

---

# DW-P0Y-002 — Plugin Manifest Schema, no runtime execution

## Goal

Implement a runtime plugin manifest schema / validator / summarizer.

No runtime execution. No package install. No code load.

## Runtime files

Add:

- `runtime/src/capabilities/plugin-manifest-schema.ts`
- `runtime/test/plugin-manifest-schema.test.ts`
- fixtures under `runtime/test/fixtures/plugin-manifests/`

Update exports.

## Manifest allowed fields

- `schemaVersion`
- `pluginId`
- `name`
- `description`
- `version`
- `publisher?`
- `homepage?`
- `license?`
- `capabilities`
- `permissions?`
- `entrypoints?` metadata only
- `packageSummary?`
- `riskNotes?`
- `tags?`

## Capability allowed fields

- `capabilityId`
- `kind: "read" | "write" | "network" | "ui" | "analysis" | "custom"`
- `summary`
- `riskLevel`
- `requiresApproval`
- `executionMode: "disabled" | "metadata_only" | "simulated"`

## Forbidden fields

Block at any depth:

- raw code
- source code content
- bundle content
- install script
- postinstall
- preinstall
- lifecycle script
- shell command
- exec/spawn/child_process
- native module
- binary blob
- API key / token / Authorization
- env value
- raw prompt / raw source / raw diff
- desktop action
- native bridge
- filesystem write request
- network credential

## Validation blocks

- missing pluginId/name/version
- duplicate capability IDs
- unsafe pluginId
- unknown schemaVersion
- executionMode not disabled/metadata_only/simulated
- capability claims broad mutation
- entrypoint path unsafe
- lifecycle script field present
- package URL with query secret
- secret marker
- raw code marker
- binary-looking manifest field
- any readiness flag true for execution

## Output

Summary-only:

- `status: parsed | warning | blocked`
- `manifestId`
- `pluginId`
- `capabilityCount`
- `permissionCount`
- `riskLevel`
- `capabilitySummaries`
- `findingCounts`
- `manifestHash`
- readiness all execution false
- source: `runtime_plugin_manifest_schema`

## Docs

Add:

- `docs/runtime-plugin-manifest-schema-v0.20.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/plugin-manifest-schema.test.ts
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add plugin manifest schema
```

---

# DW-P0Y-003 — Skill Manifest Schema, no runtime execution

## Goal

Implement runtime skill manifest schema / validator / summarizer.

No skill runtime execution.

## Runtime files

Add:

- `runtime/src/capabilities/skill-manifest-schema.ts`
- `runtime/test/skill-manifest-schema.test.ts`
- fixtures under `runtime/test/fixtures/skill-manifests/`

Update exports.

## Skill manifest allowed fields

- `schemaVersion`
- `skillId`
- `name`
- `description`
- `version`
- `author?`
- `inputSchemaSummary?`
- `outputSchemaSummary?`
- `steps?` metadata only
- `requiredCapabilities?`
- `riskNotes?`
- `simulationExamples?` summary-only
- `tags?`

## Forbidden fields

Block:

- raw prompt template with secrets
- raw tool args
- raw code
- shell command
- git command
- direct filesystem write
- network credentials
- API key / token / Authorization
- native bridge
- desktop action
- arbitrary MCP tool invocation
- plugin execution request
- lifecycle scripts

## Validation blocks

- missing skillId/name/version
- unsafe IDs
- step claims execution
- step claims mutation without disabled mode
- required capability unknown/high-risk without manual-only mode
- raw prompt/source/diff fields
- secret markers
- duplicate steps/capabilities
- readiness flags true for execution

## Output

Summary-only:

- `status`
- `skillId`
- `stepCount`
- `requiredCapabilityCount`
- `riskLevel`
- `summaryHash`
- readiness execution false
- source: `runtime_skill_manifest_schema`

## Docs

Add:

- `docs/runtime-skill-manifest-schema-v0.20.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/skill-manifest-schema.test.ts runtime/test/plugin-manifest-schema.test.ts
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add skill manifest schema
```

---

# DW-P0Y-004 — Package Metadata Scanner, no code execution

## Goal

Scan plugin / skill package metadata without executing package code.

## Runtime files

Add:

- `runtime/src/capabilities/package-metadata-scanner.ts`
- `runtime/test/package-metadata-scanner.test.ts`
- summary-only fixtures under `runtime/test/fixtures/capability-packages/`

## Input

Metadata-only input:

- package name
- version
- manifest JSON
- file list summary
- package size
- hash prefixes
- declared scripts summary
- dependency names only
- license summary

No raw package contents.

## Scanner behavior

- validate manifest with plugin/skill schema
- scan file list paths
- detect dangerous files/scripts
- detect dependency risks
- classify package risk
- produce summary-only report

## Blocks

- lifecycle scripts present
- binary/native module markers
- unsafe paths
- `.env`, secret files
- postinstall/preinstall/install scripts
- node-gyp/native build markers
- shell script entrypoints
- raw code fields
- secret markers
- oversized package
- execution flags true

## Output

- `status: scanned | warning | blocked`
- counts
- riskLevel
- manifest refs
- findings
- packageHash
- readiness execution false
- source: `runtime_package_metadata_scanner`

## Docs

Add:

- `docs/runtime-package-metadata-scanner-v0.20.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/package-metadata-scanner.test.ts runtime/test/plugin-manifest-schema.test.ts runtime/test/skill-manifest-schema.test.ts
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add capability package metadata scanner
```

---

# DW-P0Y-005 — Sandbox Contract + Built-in Safe Skill Simulation, no arbitrary execution

## Goal

Define sandbox contract and implement only built-in safe skill simulation, no arbitrary execution.

## Runtime files

Add:

- `runtime/src/capabilities/plugin-skill-sandbox-contract.ts`
- `runtime/src/capabilities/builtin-safe-skill-simulation.ts`
- `runtime/test/plugin-skill-sandbox-contract.test.ts`
- `runtime/test/builtin-safe-skill-simulation.test.ts`

## Sandbox contract

Must define:

- allowed execution modes: `disabled`, `metadata_only`, `simulated_builtin_safe`
- denied modes: arbitrary code, package runtime, shell, native, desktop
- input/output summary policy
- timeout/output budget for future
- no network default
- no filesystem write
- no EventStore write

## Built-in safe simulations

Allow only non-mutating, hardcoded simulations such as:

- summarize manifest risk
- classify capability risk
- validate required input summary
- generate documentation checklist

No dynamic code execution.

## Blocks

- custom code
- eval/function constructor
- import external package
- shell/process
- network/fetch
- filesystem write
- raw source/prompt/API key
- mutation request

## Docs

Add:

- `docs/runtime-plugin-skill-sandbox-contract-v0.20.md`
- `docs/runtime-builtin-safe-skill-simulation-v0.20.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/plugin-skill-sandbox-contract.test.ts runtime/test/builtin-safe-skill-simulation.test.ts runtime/test/package-metadata-scanner.test.ts
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add plugin skill sandbox contract
```

---

# DW-P0Y-006 — Capability Broker Integration with Plugin / Skill Metadata

## Goal

Integrate plugin / skill manifests and scanned package metadata into Capability Broker as disabled / metadata-only / simulated descriptors.

No real plugin/skill execution.

## Runtime files

Add:

- `runtime/src/capabilities/external-plugin-skill-descriptors.ts`
- `runtime/test/external-plugin-skill-descriptors.test.ts`

Update existing capability broker exports if needed.

## Descriptor requirements

Each descriptor must include:

- capabilityId
- sourceType: plugin | skill
- executionMode: disabled | metadata_only | simulated_builtin_safe
- riskLevel
- invokePolicy: disabled or manual-only preview
- summary
- manifestRef
- packageScanRef?
- redactionSummary
- no raw metadata

## Broker integration

- register descriptors read-only
- block duplicate IDs
- block execution-ready modes
- map risk and policy
- no PermissionLease issuing
- no invocation
- no EventStore write

## Docs

Add:

- `docs/runtime-plugin-skill-broker-integration-v0.20.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/external-plugin-skill-descriptors.test.ts runtime/test/capability-broker-v2.test.ts runtime/test/plugin-manifest-schema.test.ts runtime/test/skill-manifest-schema.test.ts
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): integrate plugin skill descriptors
```

---

# DW-P0Y-007 — App Plugin / Skill Host Surface, read-only

## Goal

Add App read-only surface for plugin / skill manifests, package metadata scan, sandbox contract, and broker descriptors.

No runtime execution.

## App files

Add:

- `app/src/plugin-skill-host-view.ts`

Update:

- `app/src/App.tsx`
- `app/test/desktop-shell.test.ts`

## App UI

Add panel:

```text
Plugin / Skill Host
```

Badges:

```text
Read-only / no plugin execution
Metadata only / no skill runtime
```

Inputs:

- paste plugin manifest JSON
- paste skill manifest JSON
- paste package metadata summary JSON

Buttons:

- Preview Plugin Manifest
- Preview Skill Manifest
- Preview Package Metadata
- Clear

Disabled placeholders:

- Install Plugin (disabled)
- Run Skill (disabled)
- Execute Plugin Capability (disabled)

No enabled execution buttons.
No Tauri invoke.
No EventStore write.
No fetch/network.

Display:

- manifest status
- capability counts
- risk level
- scanner findings
- sandbox mode
- broker descriptor preview
- readiness flags all false

## Docs

Add:

- `docs/app-shell-plugin-skill-host-v0.20.md`

Update app README/docs index.

## Commands

```powershell
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(app): add plugin skill host surface
```

---

# DW-P0Y-008 — Plugin / Skill Redaction / Boundary Audit + Smoke

## Goal

Add redaction / boundary audit for plugin / skill metadata and smoke coverage.

## Runtime files

Add:

- `runtime/src/capabilities/plugin-skill-redaction-audit.ts`
- `runtime/test/plugin-skill-redaction-audit.test.ts`

## App files

Add or update:

- `app/src/plugin-skill-redaction-audit-view.ts`
- `app/src/App.tsx`
- `app/test/desktop-shell.test.ts`

## Audit must block

- raw source
- raw package file contents
- raw prompt
- raw args
- API key / Authorization / bearer / secret
- install scripts
- shell commands
- native bridge
- desktop action
- arbitrary execution flags

## Output

Summary-only:

- counts
- risk categories
- redaction flags
- hash prefixes
- readiness all false

## App UI

Panel:

```text
Plugin / Skill Redaction Audit
```

Badge:

```text
Summary only / no raw metadata
```

No execution.

## Docs

Add:

- `docs/runtime-plugin-skill-redaction-audit-v0.20.md`
- `docs/app-shell-plugin-skill-redaction-audit-v0.20.md`
- `docs/plugin-skill-host-smoke-v0.20.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/plugin-skill-redaction-audit.test.ts runtime/test/package-metadata-scanner.test.ts runtime/test/external-plugin-skill-descriptors.test.ts
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add plugin skill redaction audit
```

---

# DW-P0Y-009 — v0.21 RC polish + full gates + push/tag/release

## Goal

Finalize, test, push, tag, and release v0.21.

## Release tag

```text
v0.21.0-plugin-skill-sandbox-mvp-rc.1
```

## Release title

```text
v0.21.0-plugin-skill-sandbox-mvp-rc.1 — Plugin and skill sandbox MVP, no arbitrary execution
```

## Files

Add:

- `docs/release-notes-v0.21.0-plugin-skill-sandbox-mvp-rc.1.md`
- `docs/plugin-skill-sandbox-manual-qa.md`
- `docs/plugin-skill-sandbox-rc-checklist.md`

Update:

- `README.md`
- `docs/README.md`
- `app/README.md`
- `app/src/App.tsx`
- `app/test/desktop-shell.test.ts`

## Release notes must include

Scope:

- Plugin / Skill Sandbox ADR
- Plugin Manifest Schema
- Skill Manifest Schema
- Package Metadata Scanner
- Sandbox Contract
- Built-in Safe Skill Simulation
- Capability Broker Integration
- App Plugin / Skill Host Surface
- Redaction / Boundary Audit

Working flow:

```text
Plugin and skill manifests can be parsed and risk-classified.
Package metadata can be scanned summary-only.
Capability Broker can preview plugin/skill descriptors.
App can display plugin/skill metadata read-only.
Built-in safe skill simulation is summary-only and non-mutating.
No arbitrary plugin code is executed.
No arbitrary skill runtime is executed.
```

Explicit non-goals:

- no arbitrary plugin code execution
- no arbitrary skill runtime execution
- no plugin installation with code execution
- no mutating plugin/skill tools
- no desktop action
- no native bridge
- no arbitrary process spawn
- no arbitrary shell
- no broad PermissionLease
- no autonomous agent tool execution

Safety:

- manifest schema validation
- package metadata scan
- lifecycle script detection
- native/binary detection
- redaction audit
- Broker risk classification
- disabled/manual-only policies
- App read-only surface
- summary-only reports

## Manual QA must include

- Convert smoke
- Plugin manifest safe preview
- Plugin manifest unsafe script blocked
- Skill manifest safe preview
- Skill manifest execution claim blocked
- Package metadata scanner blocks lifecycle script
- Built-in safe skill simulation preview only
- Broker descriptors read-only
- App disabled install/run/execute buttons
- No raw metadata/API key shown

## Full stage-end gates

Run all:

```powershell
pnpm install
pnpm lint
pnpm typecheck
pnpm test
pnpm test:conformance:dry
pnpm test:conformance:live
pnpm app:typecheck
pnpm app:test
pnpm app:smoke
pnpm app:preflight
pnpm app:qa:check
pnpm app:build
cargo check --manifest-path app/src-tauri/Cargo.toml
pnpm --filter @deepseek-workbench/browser-extension build
pnpm --filter @deepseek-workbench/browser-extension test
pnpm eval:web-table-to-csv
pnpm verify:v0.1-slice
pnpm release:smoke
pnpm check:boundaries
pnpm check:secrets
pnpm verify:ci
```

## Commit

```text
docs(app): release v0.21 plugin skill sandbox mvp
```

## Push / tag / release

After commit and clean tree:

```powershell
git status --short
git log --oneline origin/main..HEAD
git push origin main
```

Wait for GitHub Actions on main to be green.

Tag:

```powershell
git tag v0.21.0-plugin-skill-sandbox-mvp-rc.1
git push origin v0.21.0-plugin-skill-sandbox-mvp-rc.1
```

Wait for tag Actions to be green.

Create GitHub prerelease:

```powershell
gh release create v0.21.0-plugin-skill-sandbox-mvp-rc.1 `
  --title "v0.21.0-plugin-skill-sandbox-mvp-rc.1 — Plugin and skill sandbox MVP, no arbitrary execution" `
  --notes-file docs/release-notes-v0.21.0-plugin-skill-sandbox-mvp-rc.1.md `
  --prerelease
```

Verify:

```powershell
gh release view v0.21.0-plugin-skill-sandbox-mvp-rc.1
```

Release notes should use full docs links, for example:

```markdown
Manual GUI QA should follow
[`docs/plugin-skill-sandbox-manual-qa.md`](https://github.com/Mubai0628/DG/blob/v0.21.0-plugin-skill-sandbox-mvp-rc.1/docs/plugin-skill-sandbox-manual-qa.md).
```

## Final completion report

```text
目标：v0.21 Plugin / Skill Sandbox MVP
状态：

完成的子任务：
- DW-P0X-009:
- DW-P0Y-001:
- DW-P0Y-002:
- DW-P0Y-003:
- DW-P0Y-004:
- DW-P0Y-005:
- DW-P0Y-006:
- DW-P0Y-007:
- DW-P0Y-008:
- DW-P0Y-009:

关键文件变更：
- ...

运行命令：
- scoped checks:
- full gates:
- GitHub Actions:
- release commands:

测试结果：
- runtime tests:
- app tests:
- Rust/Tauri tests:
- conformance:
- eval:
- release smoke:
- verify:ci:

Plugin / Skill Sandbox MVP 摘要：
- plugin manifest schema:
- skill manifest schema:
- package metadata scanner:
- sandbox contract:
- built-in safe simulation:
- broker integration:
- App surface:
- redaction audit:

关键不变量验证：
- no arbitrary plugin code execution:
- no arbitrary skill runtime execution:
- no plugin lifecycle script execution:
- no mutating plugin/skill tools:
- no native bridge:
- no desktop action:
- no arbitrary shell/process spawn:
- no broad PermissionLease:
- summary-only metadata:
- redaction audit:
- Convert still works:
- approved apply/rollback still works:
- Git/shell safe lanes still fixed:
- MCP read-only tool execution still bounded:
- verify:ci passes:
- release:smoke passes:

Git：
- final commit:
- pushed main:
- tag:
- release URL:
- git status --short:

仓库卫生：
- ignored dirs remain ignored:
- generated artifacts not committed:

未完成/阻塞：
- ...

下一建议阶段：
- P0Z Fixed Multi-Agent Execution MVP
```

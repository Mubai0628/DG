# v0.21 Plugin / Skill Sandbox Post-Release Review

Release:

- tag: `v0.21.0-plugin-skill-sandbox-mvp-rc.1`
- title: Plugin and skill sandbox MVP, no arbitrary execution
- commit: `15f32da`
- pre-release: true

Checks:

- Scoped P0Y checks passed before the RC polish commit.
- Stage-end full gates passed before release.
- GitHub Actions status: green for pushed `main` and the release tag at release
  time.
- Manual GUI QA: documented in `docs/plugin-skill-sandbox-manual-qa.md`; user
  or release operator confirmation remains the source of truth for manual visual
  completion.

Current capabilities:

- Convert: `web_table_to_csv` remains the real local conversion flow.
- App-side approved apply / rollback exists behind human approval, typed
  confirmation, path/content gates, and checkpointing.
- Git / shell safe lanes exist as fixed, bounded, summary-only verification
  lanes.
- Live proposal generation exists as explicit opt-in and does not auto-apply.
- Project Knowledge exists as human-reviewed, summary-only memory.
- MCP read-only connection exists for metadata discovery.
- MCP read-only tool execution exists only for read-only, allowlisted, approved,
  bounded tool calls.
- Plugin and skill manifest and metadata governance exists with manifest schema
  validation, package metadata scanning, sandbox contract summaries, broker
  descriptors, App read-only host surface, and redaction audit.

Disabled capabilities:

- arbitrary plugin execution.
- arbitrary skill runtime execution.
- Mutating MCP tools.
- Dynamic agent bidding.
- Arbitrary agent creation.
- Desktop action.
- Native bridge.
- Autonomous arbitrary tool execution.
- Arbitrary Git or shell execution.
- Broad PermissionLease.
- Auto-apply.

Known warnings:

- Tauri bundle id warning.
- Vite chunk-size warning.
- GitHub Actions Node.js 20 deprecation annotation if present.

Release hygiene:

- No raw source persisted in plugin/skill events.
- No raw diff persisted in plugin/skill events.
- No raw prompt persisted in plugin/skill events.
- No raw package content persisted in plugin/skill events.
- No raw args or raw output persisted in plugin/skill events.
- No API key, Authorization, bearer token, or secret persistence.
- Generated artifacts and conformance outputs were not committed.

Next stage:

- P0Z moves from early Agent Route Preview / Agent Dossier surfaces to a fixed
  multi-agent execution MVP with fixed roles, fixed routes, summary-only
  dossiers, Capability Broker gated planning, human approval for apply, and
  replayable summary events.

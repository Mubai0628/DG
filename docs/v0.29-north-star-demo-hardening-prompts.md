# v0.29 North Star Demo Hardening / Policy / Safety RC 阶段提示词

任务目标：完成所有小阶段任务。

本阶段目标是完成并发布：

`v0.29.0-north-star-demo-hardening-rc.1`

阶段主题：

**North Star Demo Hardening / Policy / Safety RC**

v0.28 已经达到 North Star Demo RC 口径：跨面工作流能够把 DeepSeek proposal、固定多 Agent、Project Knowledge、MCP read-only evidence、plugin/skill metadata、desktop observer、desktop action proposal、approved desktop action、approved workspace apply/rollback、Git/shell verification 和统一 replay/audit timeline 串起来。

v0.29 不继续扩大动作半径。
本阶段只做 hardening / policy / safety / recovery：

```text
Cross-surface workflow failure recovery
PermissionLease / approval receipt consistency hardening
Capability risk / policy enforcement hardening
Replay / audit completeness checks
Evidence freshness / drift checks
Multi-agent handoff failure handling
Long-running workflow state review
North Star Demo QA matrix
Release smoke hardening
```

---

## 0. 当前状态

当前已发布：

- `v0.28.0-cross-surface-agent-workflow-rc.1`
- Release title: `Cross-surface agent workflow RC`

已完成能力：

- v0.14 End-to-end coding task MVP
- v0.15 Hardening / recovery / regression
- v0.16 Production memory / project knowledge
- v0.17 Capability host descriptor preview
- v0.18 MCP read-only connection
- v0.19 MCP tool invocation proposal / approval design
- v0.20 Controlled MCP read-only tool execution
- v0.21 Plugin / Skill sandbox manifest + metadata governance
- v0.22 Fixed multi-agent execution MVP
- v0.23 Desktop Observer MVP
- v0.24 Desktop Action Proposal MVP
- v0.25 Approved narrow desktop action execution
- v0.26 Expanded desktop action proposal gate
- v0.27 Approved expanded desktop action execution, narrow click/type only
- v0.28 Cross-surface Agent Workflow RC

当前仍然必须保持的边界：

```text
no dynamic agent bidding
no autonomous tool execution
no mutating MCP tools
no arbitrary plugin/skill runtime
no broad desktop action
no clipboard/file dialog automation
no hidden/background action
no remote control
no broad native bridge
no arbitrary Git/shell
no auto-apply
no broad PermissionLease
no raw prompt/source/diff/response/reasoning/API key in events or replay
```

---

## 1. 阶段全局原则

整个 v0.29 阶段禁止：

- 新增 broad execution capability
- 新增 dynamic agent bidding
- 新增 autonomous arbitrary tool use
- 新增 mutating MCP tool execution
- 新增 arbitrary plugin / skill runtime execution
- 扩大 desktop action 半径
- clipboard write
- file dialog automation
- drag/drop execution
- hidden / background desktop control
- remote control
- broad native bridge
- arbitrary Git / shell
- auto-apply
- raw content in events/replay/telemetry
- API key / Authorization / bearer / token persistence
- bypass existing approval / receipt / replay chains

本阶段允许：

- 新增 pure runtime hardening helpers
- 新增 App read-only / controlled summary surfaces
- 新增 consistency checks
- 新增 replay / audit completeness validation
- 新增 freshness / drift checks
- 新增 QA matrix and smoke hardening
- 新增 docs / threat model / release notes
- 对既有行为做安全收窄

---

## 2. 测试策略

普通小阶段只跑 scoped checks。

禁止普通小阶段运行：

```text
pnpm test                       # 无过滤全量
pnpm verify:ci
pnpm release:smoke
pnpm app:build
browser-extension build/test
eval:web-table-to-csv
full conformance matrix
```

Runtime tests 必须优先使用显式 Vitest 文件路径，例如：

```powershell
pnpm exec vitest run runtime/test/<specific>.test.ts
```

阶段收口 / release 小阶段必须跑 full gates。

---

## 3. Git 工作流

每个子任务开始时运行：

```powershell
git status --short
git status -sb
git log --oneline origin/main..HEAD
```

如果存在与当前子任务无关的 dirty changes，停止并报告。

每个子任务结束时：

```powershell
git diff --check
git diff --cached --check
```

然后只 stage 本任务文件并 commit。

普通子任务禁止：

```text
git push
git tag
gh release create
```

最终 RC 子任务才允许 push/tag/release。

---

## 4. v0.29 子任务列表

必须按顺序完成：

1. `DW-P1F-010` — v0.28 post-release review + P1G roadmap lock
2. `DW-P1G-001` — North Star Demo Hardening ADR / Threat Model / Implementation Gate
3. `DW-P1G-002` — Cross-surface Workflow Failure Recovery Contract
4. `DW-P1G-003` — PermissionLease / Approval Receipt Consistency Hardening
5. `DW-P1G-004` — Capability Risk / Policy Enforcement Hardening
6. `DW-P1G-005` — Replay / Audit Completeness Checks
7. `DW-P1G-006` — Evidence Freshness / Drift Checks
8. `DW-P1G-007` — Multi-Agent Handoff Failure Handling + Long-running State Review
9. `DW-P1G-008` — North Star Demo QA Matrix / Release Smoke Hardening
10. `DW-P1G-009` — v0.29 RC polish + full gates + push/tag/release

---

# DW-P1F-010 — v0.28 post-release review + P1G roadmap lock

## Goal

Create v0.28 post-release review and lock the P1G North Star Demo Hardening roadmap.

## Scope

Docs / roadmap / docs-lock tests only.

No runtime feature.
No App execution change.
No workflow execution change.

## Files

Add:

- `docs/v0.28-cross-surface-agent-workflow-postrelease-review.md`
- `docs/p1g-north-star-demo-hardening-roadmap.md`
- `docs/p1g-001-north-star-hardening-gate-plan.md`

Update:

- `docs/README.md`
- `README.md`
- `app/test/desktop-shell.test.ts` with lightweight docs-lock test

## Required content

Post-release review must record:

- tag: `v0.28.0-cross-surface-agent-workflow-rc.1`
- title: `Cross-surface agent workflow RC`
- commit: if unable to verify locally, use `to be verified`
- current capabilities:
  - Convert
  - approved execution / rollback
  - Git/shell verification safe lanes
  - project knowledge
  - MCP read-only tool execution
  - plugin/skill metadata governance
  - fixed multi-agent
  - desktop observer
  - desktop action proposal
  - approved narrow/expanded desktop actions
  - cross-surface workflow planner / preview / replay
- disabled capabilities:
  - dynamic agent bidding
  - autonomous arbitrary tool use
  - mutating MCP tools
  - arbitrary plugin/skill runtime
  - broad desktop actions
  - clipboard/file dialog automation
  - broad native bridge
  - arbitrary Git/shell
  - auto-apply

P1G roadmap must define:

- goal: harden North Star Demo without expanding execution scope
- tasks:
  - ADR / threat model
  - failure recovery contract
  - approval/receipt consistency
  - policy enforcement
  - replay/audit completeness
  - evidence freshness/drift
  - multi-agent handoff failure handling
  - QA matrix/smoke
  - RC release
- deferred:
  - dynamic bidding
  - mutating MCP tools
  - arbitrary plugin/skill execution
  - broad desktop actions
  - clipboard/file dialog automation
  - broad native bridge
  - autonomous agent control

## Commands

```powershell
pnpm lint
pnpm app:test
git diff --check
git diff --cached --check
```

## Commit

```text
docs: lock p1g north star demo hardening roadmap
```

---

# DW-P1G-001 — North Star Demo Hardening ADR / Threat Model / Implementation Gate

## Goal

Define the hardening strategy for the North Star Demo without expanding capability scope.

## Scope

Docs / design only.

## Files

Add:

- `docs/adr/0011-north-star-demo-hardening.md`
- `docs/north-star-demo-hardening-threat-model-v0.28.md`
- `docs/north-star-demo-hardening-implementation-gate-v0.28.md`
- `docs/p1g-002-cross-surface-failure-recovery-contract-plan.md`

Update:

- `docs/README.md`
- `app/test/desktop-shell.test.ts`

## ADR must decide

- v0.29 hardens existing cross-surface demo workflow.
- No new broad execution.
- All existing execution remains gated by:
  - human approval
  - typed confirmation
  - receipts / leases / scope
  - path / target / risk / freshness checks
  - replay / audit completeness
- Failures must become explicit states, not hidden logs.
- Replay / audit must be able to explain what happened and what did not happen.
- Recovery suggestions must not execute automatically.

## Threat model must cover

- partial workflow failure
- stale evidence
- stale desktop target
- stale workspace snapshot
- inconsistent approval receipt
- missing replay event
- event ordering mismatch
- missing rollback path
- agent handoff mismatch
- capability policy drift
- MCP metadata drift
- plugin/skill descriptor drift
- desktop observer mismatch
- raw artifact leakage in replay/audit
- overconfident UI readiness
- failed manual QA false pass

## Implementation gate must be testable

Categories:

- failure recovery
- approval consistency
- capability policy enforcement
- replay completeness
- evidence freshness
- agent handoff safety
- long-running state review
- App UI readiness wording
- boundary/secrets
- CI/release smoke

## Commands

```powershell
pnpm lint
pnpm app:test
git diff --check
git diff --cached --check
```

## Commit

```text
docs: add north star demo hardening adr
```

---

# DW-P1G-002 — Cross-surface Workflow Failure Recovery Contract

## Goal

Add runtime summary-only contract for cross-surface workflow failure recovery.

## Runtime module

Add:

- `runtime/src/workflows/cross-surface-failure-recovery.ts`

Update exports:

- `runtime/src/workflows/index.ts` if exists
- `runtime/src/index.ts`

## Types

Define:

- `CrossSurfaceFailureRecoveryInput`
- `CrossSurfaceFailureRecoveryPlan`
- `CrossSurfaceFailureKind`
- `CrossSurfaceRecoveryAction`
- `CrossSurfaceRecoveryStatus`
- `CrossSurfaceRecoveryFinding`
- `CrossSurfaceRecoveryReadiness`
- `buildCrossSurfaceFailureRecoveryPlan(input)`
- `summarizeCrossSurfaceFailureRecoveryPlan(plan)`
- `validateCrossSurfaceFailureRecoveryInput(input)`

## Failure kinds

Include:

- `proposal_generation_failed`
- `schema_repair_failed`
- `validation_blocked`
- `approval_missing`
- `apply_failed`
- `verification_failed`
- `rollback_required`
- `rollback_failed`
- `mcp_evidence_stale`
- `plugin_skill_metadata_stale`
- `desktop_observer_stale`
- `desktop_action_blocked`
- `agent_handoff_failed`
- `replay_incomplete`
- `policy_mismatch`

## Recovery actions

All actions are advisory only:

- `retry_proposal_generation`
- `refresh_workspace_index`
- `refresh_project_knowledge`
- `refresh_mcp_metadata`
- `refresh_plugin_skill_metadata`
- `refresh_desktop_observation`
- `request_human_approval`
- `run_rollback`
- `rerun_verification`
- `inspect_replay_gap`
- `stop_and_report`

No action may execute.

## Validation blocks

- raw source/diff/prompt/response/API key
- execution fields
- auto-retry that would call model
- auto-rollback
- auto-apply
- Git/shell execution
- desktop action execution
- EventStore write request
- hidden background action

## Output

Summary-only:

- plan id
- failure kind counts
- recommended next action
- blockers/warnings
- no raw content
- readiness flags all false for execution

## Tests

Add:

- `runtime/test/cross-surface-failure-recovery.test.ts`

Cover:

- each failure kind maps to advisory action
- rollback_required recommends rollback but does not execute
- replay_incomplete recommends inspect
- policy mismatch blocks automation
- raw/API key blocked
- all execution flags false

## Docs

Add:

- `docs/runtime-cross-surface-failure-recovery-v0.28.md`

Update docs index.

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/cross-surface-failure-recovery.test.ts
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add cross-surface failure recovery contract
```

---

# DW-P1G-003 — PermissionLease / Approval Receipt Consistency Hardening

## Goal

Add runtime/App consistency checks between approval receipts, PermissionLease previews, apply/rollback receipts, MCP tool approvals, desktop action approvals, and workflow stages.

## Runtime module

Add:

- `runtime/src/workflows/approval-consistency-check.ts`

## Types

Define:

- `ApprovalConsistencyInput`
- `ApprovalConsistencyReport`
- `ApprovalConsistencyScope`
- `ApprovalConsistencyFinding`
- `ApprovalConsistencyStatus`
- `ApprovalConsistencyReadiness`
- `buildApprovalConsistencyReport(input)`
- `summarizeApprovalConsistencyReport(report)`

## Check consistency for

- user workspace apply receipt
- rollback receipt
- MCP read-only tool approval
- desktop action approval
- model proposal session receipt
- live proposal opt-in policy
- capability plan / broker preview
- workflow scenario id
- task id
- workspace root ref
- allowed paths
- target refs
- expiration
- typed confirmation
- max files / max bytes
- no broad lease

## Must block

- expired receipt
- mismatched task id
- mismatched workspace root ref
- mismatched proposal id
- receipt claims apply but stage is desktop action
- desktop action receipt used for workspace apply
- MCP approval used for plugin/skill action
- PermissionLease preview claims broad mutation
- broad wildcard scope
- missing typed confirmation where required
- any raw/API key field
- any execution readiness true from report

## App integration

Add:

- `app/src/approval-consistency-view.ts`

Panel:

- “Approval / Receipt Consistency”
- read-only / advisory
- no approve/reject/apply/rollback buttons
- shows consistent/inconsistent stage count

## Tests

Runtime:

- `runtime/test/approval-consistency-check.test.ts`

App:

- docs/source/UI locks

## Docs

Add:

- `docs/runtime-approval-consistency-check-v0.28.md`
- `docs/app-shell-approval-consistency-v0.28.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/approval-consistency-check.test.ts
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add approval consistency checks
```

---

# DW-P1G-004 — Capability Risk / Policy Enforcement Hardening

## Goal

Add runtime policy enforcement report for cross-surface capability usage.

## Runtime module

Add:

- `runtime/src/workflows/capability-policy-enforcement.ts`

## Inputs

- capability descriptors
- broker plan previews
- MCP descriptors
- plugin/skill descriptors
- desktop action proposals
- Git/shell lane summaries
- approval consistency report
- risk policy

## Must enforce

- disabled capability cannot execute
- manual-only requires explicit approval
- read-only cannot mutate
- MCP mutating tools blocked
- plugin/skill runtime execution blocked unless explicitly supported later
- desktop actions outside allowlist blocked
- arbitrary Git/shell blocked
- broad PermissionLease blocked
- App execution not enabled by readiness text alone

## Output

Summary-only policy report:

- policyId
- allowedCount
- blockedCount
- warningCount
- capability category counts
- risk counts
- enforcementHash
- no raw args/output

## App integration

Add:

- `app/src/capability-policy-enforcement-view.ts`

Panel:

- read-only policy report
- shows blocked/warning counts
- no execute/approve buttons

## Tests

Runtime:

- `runtime/test/capability-policy-enforcement.test.ts`

App:

- UI/docs locks

## Docs

Add:

- `docs/runtime-capability-policy-enforcement-v0.28.md`
- `docs/app-shell-capability-policy-enforcement-v0.28.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/capability-policy-enforcement.test.ts
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add capability policy enforcement
```

---

# DW-P1G-005 — Replay / Audit Completeness Checks

## Goal

Add summary-only replay/audit completeness checker for cross-surface workflow.

## Runtime module

Add:

- `runtime/src/workflows/replay-audit-completeness.ts`

## Checks

Ensure workflow replay contains required summaries for:

- task / run draft
- model proposal
- repair/schema validation
- approval receipt
- apply result
- verification result
- rollback result if rollback occurred
- MCP evidence if referenced
- plugin/skill evidence if referenced
- desktop observation if desktop action referenced
- desktop action proposal
- desktop action execution if occurred
- agent route / handoff summaries
- policy enforcement summary
- redaction audit summary

## Must block / warn

- missing required event
- out-of-order event
- duplicate conflicting event id
- apply without approval
- rollback without checkpoint
- desktop action without observer evidence
- MCP tool result without approval
- raw content in event
- API key in event
- replay claims execution but result missing

## App integration

Add/update:

- `app/src/cross-surface-replay-audit-view.ts`

Panel:

- “Cross-surface Replay / Audit Completeness”
- read-only
- no replay re-execution
- no apply/rollback

## Tests

Runtime:

- `runtime/test/replay-audit-completeness.test.ts`

App:

- UI/docs/source locks

## Docs

Add:

- `docs/runtime-replay-audit-completeness-v0.28.md`
- `docs/app-shell-cross-surface-replay-audit-v0.28.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/replay-audit-completeness.test.ts
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add replay audit completeness checks
```

---

# DW-P1G-006 — Evidence Freshness / Drift Checks

## Goal

Add summary-only freshness and drift checks for evidence used in cross-surface workflows.

## Runtime module

Add:

- `runtime/src/workflows/evidence-freshness-drift.ts`

## Evidence categories

- workspace index
- project knowledge
- memory recall
- MCP metadata
- MCP tool result
- plugin/skill metadata
- desktop observation
- desktop action target
- Git status/diff
- shell verification
- agent handoff dossier

## Checks

- stale timestamp
- hash mismatch
- evidence ref missing
- source type mismatch
- desktop observation too old
- workspace snapshot outdated
- MCP descriptor changed
- plugin/skill metadata changed
- Git diff changed after validation
- verification result older than apply
- agent handoff references missing context

## Output

Summary-only:

- freshnessId
- stale count
- drift count
- blocker/warning counts
- evidence category counts
- no raw evidence

## App integration

Add:

- `app/src/evidence-freshness-drift-view.ts`

Read-only panel.

## Tests

Runtime:

- `runtime/test/evidence-freshness-drift.test.ts`

App:

- docs/UI locks

## Docs

Add:

- `docs/runtime-evidence-freshness-drift-v0.28.md`
- `docs/app-shell-evidence-freshness-drift-v0.28.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/evidence-freshness-drift.test.ts
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add evidence freshness drift checks
```

---

# DW-P1G-007 — Multi-Agent Handoff Failure Handling + Long-running State Review

## Goal

Harden fixed multi-agent handoff and long-running workflow state review.

## Runtime module

Add:

- `runtime/src/agents/fixed-agent-handoff-review.ts`
- or `runtime/src/workflows/agent-handoff-state-review.ts`

## Checks

- missing role output
- role order mismatch
- stale dossier hash
- missing evidence ref
- reviewer/verifier skipped
- coder output missing proposal id
- verifier result missing verification summary
- agent handoff contains raw prompt/source/diff/API key
- long-running stage stale
- interrupted workflow recovery missing nextAction

## App integration

Add:

- `app/src/agent-handoff-state-review-view.ts`

Panel:

- read-only
- no agent rerun
- no dynamic bidding
- no tool execution

## Tests

Runtime:

- `runtime/test/agent-handoff-state-review.test.ts`

App:

- UI/docs locks

## Docs

Add:

- `docs/runtime-agent-handoff-state-review-v0.28.md`
- `docs/app-shell-agent-handoff-state-review-v0.28.md`

## Commands

```powershell
pnpm --filter @deepseek-workbench/runtime build
pnpm --filter @deepseek-workbench/runtime typecheck
pnpm exec vitest run runtime/test/agent-handoff-state-review.test.ts
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
feat(runtime): add agent handoff state review
```

---

# DW-P1G-008 — North Star Demo QA Matrix / Release Smoke Hardening

## Goal

Add QA matrix and release smoke hardening for North Star Demo workflows.

## Scope

Tests/docs/scripts only.
No new execution capability.

## Add

- `docs/north-star-demo-manual-qa-matrix.md`
- `docs/north-star-demo-release-smoke-v0.28.md`
- `app/test/fixtures/north-star-demo-smoke.json`
- additional app tests for docs-lock / fixture validation
- optional script if existing test infrastructure supports it

## QA matrix must cover

- Convert
- live proposal
- fixed multi-agent route
- project knowledge recall
- MCP read-only evidence
- plugin/skill metadata evidence
- desktop observer evidence
- desktop action proposal
- approved desktop action
- approved workspace apply
- Git/shell verification
- rollback
- replay/audit
- redaction
- failure recovery

## Smoke must not

- run live DeepSeek by default
- execute broad desktop action
- execute arbitrary Git/shell
- execute MCP mutating tools
- execute plugin/skill runtime
- persist raw prompt/source/diff/API key

## Commands

```powershell
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
git diff --cached --check
```

## Commit

```text
test(app): add north star demo qa matrix
```

---

# DW-P1G-009 — v0.29 RC polish + full gates + push/tag/release

## Goal

Finalize and release v0.29.

## Release tag

```text
v0.29.0-north-star-demo-hardening-rc.1
```

## Release title

```text
v0.29.0-north-star-demo-hardening-rc.1 — North Star demo hardening, policy, and replay safety
```

## Files

Add:

- `docs/release-notes-v0.29.0-north-star-demo-hardening-rc.1.md`
- `docs/north-star-demo-hardening-manual-qa.md`
- `docs/north-star-demo-hardening-rc-checklist.md`

Update:

- `README.md`
- `docs/README.md`
- `app/README.md`
- `app/src/App.tsx`
- `app/test/desktop-shell.test.ts`

## Release notes must include

Current working flow:

```text
v0.28 cross-surface workflow remains available.
North Star demo evidence, approvals, policies, replay, freshness, and handoff checks are hardened.
No new broad execution capability is added.
Existing approved execution lanes remain bounded.
```

Non-goals:

```text
no dynamic bidding
no autonomous tool execution
no mutating MCP tools
no arbitrary plugin/skill runtime
no broad desktop action
no clipboard/file dialog automation
no native bridge
no arbitrary Git/shell
no auto-apply
```

Safety:

```text
failure recovery plan
approval consistency
policy enforcement
replay completeness
evidence freshness
agent handoff review
QA matrix
summary-only events/replay
no raw content
```

## Manual QA must include

- Convert smoke
- live proposal
- fixed agent route
- project knowledge recall
- MCP read-only evidence
- plugin/skill metadata evidence
- desktop observer evidence
- desktop action proposal
- approved desktop action
- approved workspace apply
- verification
- rollback
- replay/audit
- failure recovery
- redaction checks

## Scoped checks first

```powershell
pnpm app:typecheck
pnpm app:test
pnpm check:boundaries
pnpm check:secrets
git diff --check
```

## Full stage-end gates

Run:

```powershell
pnpm install
pnpm lint
pnpm typecheck
pnpm test
pnpm test:conformance:dry
pnpm test:conformance:live
pnpm app:typecheck
pnpm app:test
pnpm app:smoke
pnpm app:preflight
pnpm app:qa:check
pnpm app:build
cargo check --manifest-path app/src-tauri/Cargo.toml
pnpm --filter @deepseek-workbench/browser-extension build
pnpm --filter @deepseek-workbench/browser-extension test
pnpm eval:web-table-to-csv
pnpm verify:v0.1-slice
pnpm release:smoke
pnpm check:boundaries
pnpm check:secrets
pnpm verify:ci
```

## Commit

```text
docs(app): release v0.29 north star demo hardening
```

## Push / tag / release

After clean tree and gates pass:

```powershell
git push origin main
```

Wait for main GitHub Actions green.

```powershell
git tag v0.29.0-north-star-demo-hardening-rc.1
git push origin v0.29.0-north-star-demo-hardening-rc.1
```

Wait for tag Actions green.

Create GitHub prerelease:

```powershell
gh release create v0.29.0-north-star-demo-hardening-rc.1 `
  --title "v0.29.0-north-star-demo-hardening-rc.1 — North Star demo hardening, policy, and replay safety" `
  --notes-file docs/release-notes-v0.29.0-north-star-demo-hardening-rc.1.md `
  --prerelease
```

Verify:

```powershell
gh release view v0.29.0-north-star-demo-hardening-rc.1
```

Release docs links must use full `docs/...` paths.

---

# Final completion report format

At the end, report:

```text
目标：v0.29 North Star Demo Hardening / Policy / Safety RC
状态：

完成的子任务：
- DW-P1F-010:
- DW-P1G-001:
- DW-P1G-002:
- DW-P1G-003:
- DW-P1G-004:
- DW-P1G-005:
- DW-P1G-006:
- DW-P1G-007:
- DW-P1G-008:
- DW-P1G-009:

关键文件变更：
- ...

运行命令：
- scoped checks:
- full gates:
- GitHub Actions:
- release commands:

测试结果：
- runtime tests:
- app tests:
- Rust/Tauri tests:
- conformance:
- eval:
- release smoke:
- verify:ci:

North Star Demo Hardening 摘要：
- failure recovery:
- approval consistency:
- policy enforcement:
- replay/audit completeness:
- evidence freshness:
- agent handoff review:
- QA matrix:
- App UX:

关键不变量验证：
- no new broad capability:
- no dynamic bidding:
- no autonomous tool execution:
- no mutating MCP tools:
- no arbitrary plugin/skill runtime:
- no broad desktop action:
- no arbitrary Git/shell:
- no native bridge:
- no auto-apply:
- summary-only events/replay:
- Convert still works:
- approved apply/rollback still works:
- verification lanes still work:
- verify:ci passes:
- release:smoke passes:

Git：
- final commit:
- pushed main:
- tag:
- release URL:
- git status --short:

仓库卫生：
- ignored dirs remain ignored:
- generated artifacts not committed:

未完成/阻塞：
- ...

下一建议阶段：
- P1H Policy / PermissionLease Hardening
```

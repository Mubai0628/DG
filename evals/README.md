# evals

Reserved for redaction, injection, and web-table-to-CSV evaluation fixtures.

DW-P0A-001 does not add eval logic or external-content processing. Future evals must avoid storing raw DOM, raw prompt, raw screenshot, raw OCR, or secrets by default.
